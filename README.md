# Wav2Raw

Small cli tool that extracts the signal data from a .wav file and writes it out into a headerless ("raw") file.
A lot of more advanced tools exist that can do this, this one was just a small side project to learn a bit of rust.

Usage:

```
wav2raw <INFILE> <OUTFILE>
```
